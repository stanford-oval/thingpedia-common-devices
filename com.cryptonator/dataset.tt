dataset @com.cryptonator language "en" {
    query (p_currency :Entity(tt:cryptocurrency_code))  := @com.cryptonator.get_price(currency=p_currency)
    #_[utterances=["price of $p_currency",
                   "value in dollars of $p_currency",
                   "1 $p_currency in dollars",
                   "value in usd of $p_currency"]]
    #[id=1687497]
    #[name=""];

}