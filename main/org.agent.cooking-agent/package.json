{
  "name": "org.agent.cooking-agent",
  "main": "dist/index.js",
  "author": "Jackie Yang <jackie@jackieyang.me>",
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf tmp/*.log",
    "build": "npm run clean && tsc --build",
    "prepare": "npm run build"
  },
  "dependencies": {
    "string-interp": "^0.3.6"
  }
}
